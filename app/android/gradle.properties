org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
#
# Dinyalakan, tidak seperti bawaan template. AGP 9 punya dukungan Kotlin
# bawaan, dan sebagian plugin sudah berhenti memasang plugin Kotlin sendiri
# begitu melihat AGP 9 karena mengira dukungan itu menyala — file_picker
# salah satunya:
#
#     if (!isAgp9OrAbove) { apply plugin: 'org.jetbrains.kotlin.android' }
#
# Dengan flag ini mati, tidak ada satu pun yang mengompilasi sumber .kt milik
# plugin tersebut. Modulnya sendiri lolos tanpa error; kegagalannya baru
# muncul jauh di hilir sebagai "cannot find symbol: FilePickerPlugin" saat
# GeneratedPluginRegistrant dikompilasi.
android.builtInKotlin=true
